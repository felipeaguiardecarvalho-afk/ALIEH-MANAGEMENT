/** httpOnly session cookie (JWT). */
export const SESSION_COOKIE_NAME = "alieh_session";

/** Espelho de `utils/app_auth.py` — ambiente aberto sem login (só protótipo). */
export const PROTOTYPE_OPEN_ENV = "ALIEH_PROTOTYPE_OPEN";
