# Copy to `.env` (never commit). Supabase → Project Settings → API.
# Usado por scripts Node (`npm run test:customers`) e pela stack Python `alieh_supabase/`.

SUPABASE_URL=https://YOUR_PROJECT_REF.supabase.co
SUPABASE_ANON_KEY=your-anon-key-here
