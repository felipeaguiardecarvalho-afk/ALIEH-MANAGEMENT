# Streamlit Cloud: paste into App settings → Secrets (or copy to secrets.toml locally; never commit real secrets).
# Same keys as .env — use SUPABASE_ANON_KEY (not SUPABASE_KEY).

SUPABASE_URL = "https://your-project.supabase.co"
SUPABASE_ANON_KEY = "your-anon-key"
